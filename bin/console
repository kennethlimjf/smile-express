#!/usr/bin/env node

var repl = require("repl");
var context = repl.start("app-console > ").context;
var models = require("../models");

context.User = models.User
context.AdminUser = models.AdminUser
